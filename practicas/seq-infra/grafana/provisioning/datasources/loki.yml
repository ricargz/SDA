apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    uid: vulnerableapp-loki
    isDefault: true
    editable: true
    jsonData:
      timeout: 60
      maxLines: 1000
